name: chatgpt_pro_clone
description: ChatGPT-like AI App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  google_sign_in: ^6.0.0
  flutter_dotenv: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - .env